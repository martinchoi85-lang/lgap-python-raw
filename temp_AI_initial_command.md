# AI Code Generation Guidelines for python-raw-lgap-daemon

본 문서는 Antigravity IDE를 사용하는 개발자 및 협업 AI 모델이 본 프로젝트의 코드를 생성, 수정 및 리팩토링할 때 예외 없이 100% 준수해야 하는 강제 규칙 가이드입니다.

## 1. 기본 스타일 규칙
- **언어 사양**: Python 3.10 이상 문법을 준수합니다.
- **타입 힌팅**: 모든 함수의 매개변수와 반환값에는 엄격한 타입 힌팅을 적용합니다. (예: `def calculate_checksum(data: bytes) -> int:`)
- **외부 종속성 배제**: 특수한 사유가 없는 한 외부 라이브러리는 `pyserial`로 한정하며, 가급적 파이썬 표준 라이브러리(`threading`, `queue`, `socket`, `logging` 등)만을 사용하여 가볍고 순수한 코드를 지향합니다.

## 2. LGAP 시리얼 통신 특화 가이드

### 2.1 패킷 16바이트 정합성 및 프레이밍
- RS-485 버스는 언제나 불특정 바이트 노이즈가 유입될 수 있습니다. 단순 `read(16)`를 수행할 경우 프레임 싱크가 뒤틀릴 우려가 있습니다.
- **동기화 알고리즘 규칙**: 
  - 버퍼에서 바이트 단위로 읽어 들이면서, LGAP 패킷의 고유 시작 바이트 패턴(Header, 일반적으로 문헌상 정의된 특정 헤더 바이트)을 감지합니다.
  - 헤더 감지 시점부터 정확히 16바이트를 수집하여 1개의 패킷 프레임을 완성합니다.
  - 16바이트 프레임 완성 직후 즉각 XOR 체크섬을 수행하고, 유효하지 않은 패킷은 즉시 폐기하고 다음 헤더를 찾도록 설계합니다.

### 2.2 XOR 체크섬 검증 로직
- 패킷은 정확히 16바이트의 고정 길이입니다.
- **수식 규칙**:
  ```python
  # packet은 16바이트 크기의 bytes 타입 객체
  payload_sum = sum(packet[0:15]) % 256
  checksum = payload_sum ^ 0x55
  is_valid = (checksum == packet[15])



  프로젝트 매니저(PM)이자 기술 멘토로서, 이번 하드웨어 및 소프트웨어 아키텍처의 전면 피벗(Wireless ESPHome/MQTT에서 **유선 USB-to-RS485 파이썬 직접 제어**로의 전환)을 전적으로 지지합니다.

이 방식은 스마트 홈이나 단순 PoC를 넘어, **상용 수준의 산업용 현장(Industrial Grade)에서도 가장 신뢰받는 물리 계층 연결 방식**입니다. 무선 환경의 고질적인 문제인 신호 간섭이나 단절 문제를 원천 차단하고, 중간 브릿지(ESP 칩)와 프로토콜 변환(MQTT 브로커) 단계를 걷어냄으로써 지연 시간(Latency)을 최소화하고 장애 포인트를 획기적으로 줄일 수 있습니다.

본격적인 설계에 앞서, 소프트웨어 개발자로서 반드시 머릿속에 담고 가셔야 할 **핵심 하드웨어 및 통신 개념**을 간략히 짚어 드린 후, 요청하신 6가지 산출물 문서를 제시해 드리겠습니다.

---

### 💡 멘토의 하드웨어 & 통신 핵심 강의

#### 1. RS-485 반이중(Half-Duplex) 통신의 이해와 S/W적 주의점

* **원리**: RS-485는 두 개의 통신선(A, B)의 전압 차이를 이용해 데이터를 전송하는 차동 신호 방식입니다. 단, 선 두 개를 송신(TX)과 수신(RX)이 공유하므로 **동시에 데이터를 보낼 수 없습니다.**
* **S/W 제약**: 만약 라즈베리파이가 에어컨에 상태 확인 패킷(Poll)을 보내는 도중에 제어 명령(Command) 패킷을 동시에 쓰면(Write), 하드웨어 수준에서 데이터가 충돌(Collision)하여 두 패킷 모두 깨져버립니다. 따라서 **반드시 시리얼 포트에 접근하는 단 하나의 쓰기 락(Mutex Lock)을 구현**해야 합니다.

#### 2. 순차적 폴링(Passive Polling) 엔진의 동작 방식

* 에어컨 실외기/실내기 컨트롤러는 스스로 먼저 말을 걸지 않고, 오직 Master(라즈베리파이)가 물어볼 때만 응답하는 **수동적(Passive) 구조**입니다.
* 우리는 "질문 ➔ 대기(Timeout) ➔ 답변 파싱"의 사이클을 주기적으로 반복하는 루프 엔진을 설계해야 합니다.
* 사용자가 제어 명령을 내렸을 때는 이 주기적인 폴링 루프 사이를 일시 정지(Preempt)하거나, 우선순위 큐(Priority Queue)를 사용해 제어 패킷을 폴링 패킷 사이에 끼워 넣는(Injection) 영리한 스케줄링이 필요합니다.

#### 3. 절연 컨버터(Isolated Converter) 환경에서의 디버깅

* 우리가 사용하는 'USB to RS-485 Isolated Converter(절연 컨버터)'는 실외기 측의 고전압 서지나 노이즈가 라즈베리파이로 흘러 들어와 메인보드를 태워 먹는 것을 물리적으로 차단해 줍니다.
* 다행히 물리 배선과 노이즈 튜닝은 함께 협업 중인 장비 전문가가 처리하므로, 우리는 하드웨어 신호 파형(오실로스코프 등)을 신경 쓸 필요가 전혀 없습니다. 오직 **Hex 바이트 로그 데이터**만 눈으로 확인하며 로직을 구축하면 됩니다.

---

요청하신 새 프로젝트용 마크다운 산출물 6종을 아래에 차례대로 작성해 드립니다. 이 파일들을 프로젝트 루트에 배치하시면, 이후 Antigravity IDE(혹은 Cursor/Windsurf 등)가 전체 컨텍스트를 완벽하게 이해하고 안정적인 파이썬 코드를 생성할 수 있게 됩니다.

---

### 1. `PRD.md` (제품 요구사항 정의서)

```markdown
# Product Requirements Document (PRD) - Python Raw LGAP Daemon

## 1. 개요 (Overview)
- **프로젝트명**: python-raw-lgap-daemon
- **목적**: 라즈베리파이와 실외기 간의 물리적 유선 연결(RS-485)을 통해, 외부 미들웨어(ESPHome, MQTT) 없이 파이썬 단독 데몬으로 LG 시스템 에어컨(LGAP 프로토콜)의 상태를 모니터링하고 제어하는 경량 PoC(개념 검증) 엔진 개발.
- **배경**: Wi-Fi 신호 불안정 및 이기종 게이트웨이 관리의 하드웨어 리스크를 제거하고, 산업용 장비의 안정성과 현장 설치 편의성을 극대화하기 위해 직접 RS-485 시리얼 통신 제어 구조로 피벗함.

## 2. 사용자 및 협업 환경 정의
- **개발 환경**: 라즈베리파이 (Debian 기반 OS), Python 3.10+, Antigravity IDE 활용.
- **물리 배선**: USB to RS-485 Isolated Converter를 사용하여 실외기 메인 PCB의 CENA/CENB 단자에 직접 유선 연결.
- **역할 분담**: 배선 설계, 전기 신호 무결성 확보 및 물리 디버깅은 하드웨어/장비 전문가가 담당. 소프트웨어 개발자는 오직 파이썬 내부의 시리얼 바이트 송수신 및 프로토콜 로직 무결성에 집중.

## 3. 핵심 기능 요구사항 (Functional Requirements)

### F-1. 물리 시리얼 포트 제어 및 안정화
- **설정 규격**: Baud rate 4800 bps, Data bits 8, Parity None, Stop bits 1 (8N1).
- **자동 복구**: 케이블 분리, USB 컨버터 재연결 등 물리적 단절 발생 시 `serial.SerialException`을 안전하게 캐치하고, 지수 백오프(Exponential Backoff) 알고리즘을 적용하여 자동으로 포트를 재오픈 및 복구해야 함.

### F-2. LGAP 프로토콜 패킷 핸들링 (XOR Checksum)
- **패킷 규격**: 16바이트 고정 크기 바이너리 패킷.
- **체크섬 룰**: 마지막 16번째 바이트를 제외한 앞 1~15바이트의 총합을 256으로 나눈 나머지(Modulo 256)를 구한 뒤, 이 값에 0x55를 XOR 연산한 최종 1바이트를 생성 및 검증.
- **프레이밍(Framing)**: RS-485의 라인 노이즈로 인해 깨진 바이트가 유입될 수 있으므로, 패킷의 시작을 알리는 헤더 바이트 패턴을 기준으로 16바이트 동기화를 정밀하게 맞춰야 함.

### F-3. 반이중(Half-Duplex) 순차적 폴링 엔진
- **동작**: 에어컨은 수동적(Passive) 장비이므로, 라즈베리파이가 주기적으로 상태 조회(Poll) 패킷을 보내고 응답을 기다려야 함.
- **스케줄링**: 실내기 ID별로 순차적으로 폴링을 수행(예: 3~5초 간격).
- **제어 명령 주입**: 사용자의 제어 명령(온도 변경 등)이 발생하면, 현재의 폴링 사이클을 안전하게 일시 정지(Preempt)하고 제어 패킷을 우선 전송한 뒤 응답을 확인하고 폴링 루프를 재개함.

### F-4. 경량 로컬 인터페이스 (PoC 최적화)
- PoC 단계이므로 별도의 복잡한 웹 UI나 외부 브로커 연동을 구현하지 않음.
- 설정값(대상 실내기 ID 리스트, 기본 온도 등)은 `config.py`에 하드코딩하여 사용 조작을 최소화함.
- 외부 제어 테스트용으로 Unix Domain Socket 또는 단순 로컬 UDP 포트를 열어두고, 제어 명령(JSON 포맷)을 수신하면 이를 데몬 내부의 Command Queue에 밀어 넣는 방식으로 설계.

## 4. 비기능 요구사항 (Non-Functional Requirements)
- **자원 효율성**: 가벼운 파이썬 단독 데몬으로 CPU 점유율 1% 미만 유지.
- **동시성 무결성**: 동일 시리얼 채널에 대한 다중 쓰기 방지를 위해 완벽한 동시성 락(Lock) 구현.
- **무중단 동작**: `systemd` 서비스 등록을 통해 프로세스 크래시 발생 시 OS 레벨에서 자동 리스타트 보장.

```

---

### 2. `roadmap.md` (개발 로드맵)

```markdown
# Project Roadmap - python-raw-lgap-daemon

본 로드맵은 하드웨어 연동 기초 단계부터 순차 폴링 엔진 및 서비스 데몬화까지의 마일스톤을 다룹니다.

## [Phase 1] 환경 구축 및 루프백 검증 (환경 & 통신 준비)
- **목표**: 개발 환경 셋업 및 USB-to-RS485 컨버터 작동 검증.
- **세부 태스크**:
  - [ ] 라즈베리파이 가상환경(venv) 및 의존성 라이브러리(`pyserial` 등) 셋업.
  - [ ] USB-to-RS485 컨버터의 RX/TX 단자를 쇼트(Short)시켜 루프백(Loopback) 자가 테스트 코드를 작성하고 4800 bps 8N1 전송 성공 확인.
  - [ ] 하드웨어 디버깅용 시리얼 스니퍼 스크립트 작성 (원시 Hex 로그 수집 및 저장 도구 확보).

## [Phase 2] LGAP 프로토콜 코어 파서 구현 (직렬화 & 체크섬)
- **목표**: 16바이트 LGAP 패킷의 인코딩/디코딩 엔진 개발 및 단위 테스트 완료.
- **세부 태스크**:
  - [ ] LGAP 16바이트 패킷 빌더 및 구조체 매퍼 정의.
  - [ ] Modulo 256 및 XOR 0x55 알고리즘 기반의 체크섬 생성 및 검증 모듈 구현.
  - [ ] 기 구축된 에어컨 원시 시리얼 로그(Raw Hex Dump) 데이터를 Mock 데이터로 사용하여 파서의 정합성을 검증하는 pytest 단위 테스트 구축 (100% Pass 목표).

## [Phase 3] Half-Duplex 폴링 및 동시성 제어 엔진 개발
- **목표**: 패킷 충돌이 없는 순차 폴링 및 제어 명령 주입 스케줄러 구현.
- **세부 태스크**:
  - [ ] 시리얼 포트 읽기/쓰기 시 동시 접근을 막는 Thread-safe Mutex Lock 설계.
  - [ ] 설정된 실내기 ID 리스트를 주기적으로 도는 Sequential Polling Thread 구현.
  - [ ] 제어 명령 처리를 위한 Priority Command Queue 구현 및 폴링 스케줄러에 명령 주입(Injection) 로직 구현.

## [Phase 4] 실장 검증 및 예외 처리 고도화
- **목표**: 실제 에어컨 실외기 배선 연결 후 실장 테스트 및 물리 예외 방어.
- **세부 태스크**:
  - [ ] 실외기 PCB CENA/CENB 포트에 배선 연결 후 실 데이터 폴링 및 파싱 성공 여부 검증.
  - [ ] 물리 라인 노이즈로 인한 패킷 깨짐(Framing Error) 발생 시, 패킷 시작 헤더를 기준으로 슬라이딩 윈도우 방식으로 정렬 및 동기화 복구 로직 적용.
  - [ ] 응답 타임아웃(No-response) 발생 시 자동 재시도 및 슬레이브 온라인/오프라인 판정 시스템 개발.

## [Phase 5] 데몬화 및 외부 테스트 인터페이스 탑재
- **목표**: 무중단 서비스 구동 및 외부 명령 입력 연동 인터페이스 완료.
- **세부 태스크**:
  - [ ] 시스템 부팅 시 자동 기동 및 프로세스 강제 종료 시 재시작을 수행하는 `systemd` 서비스 구성 파일 작성.
  - [ ] Unix Domain Socket 혹은 가벼운 Local UDP Listener 스레드를 추가하여 로컬 CLI 환경에서 에어컨에 제어 JSON을 날릴 수 있는 수단 마련.
  - [ ] 상용 배포용 파일 구조화 및 `README.md` 가이드라인 완성.

```

---

### 3. `architecture.md` (시스템 구조 설계서)

```markdown
# Architecture Design - python-raw-lgap-daemon

본 아키텍처는 반이중(Half-Duplex) RS-485 유선 환경의 제약 조건을 극복하고, 동시성 오류 및 노이즈 유입을 차단하는 **스레드 및 큐 기반 멀티태스킹 아키텍처**입니다.

## 1. 물리 구조 및 하드웨어 토폴로지


```

[ LG 실외기 Main PCB ]
│
│ (CENA / CENB 단자) - RS-485 차동 신호 라인
▼
[ USB-to-RS-485 Isolated Converter ] (전기적 서지 차단)
│
│ (USB 버스 / 가상 시리얼 포트: /dev/ttyUSB0)
▼
[ 라즈베리파이 (Python Daemon) ]

```

## 2. 파이썬 데몬 내부 소프트웨어 컴포넌트 구조


```

+--------------------------------------------------------------------------------+
|                             Raspberry Pi Daemon                                |
|                                                                                |
|  +-----------------------+                         +------------------------+  |
|  |  Local IPC Listener   |                         |  Master Polling Thread |  |
|  |  (Unix Domain Socket) |                         |  (주기적 상태 요청)     |  |
|  +-----------+-----------+                         +-----------+------------+  |
|              |                                                 |               |
|              v Command Push                                    v Trigger       |
|    +---------+---------+                             +---------+---------+     |
|    |  Command Queue    |                             |   Serial Mutex    |     |
|    | (Priority Queue)  |                             |   (Thread Lock)   |     |
|    +---------+---------+                             +---------+---------+     |
|              |                                                 |               |
|              +----------------------->+<-----------------------+               |
|                                       |                                        |
|                                       v                                        |
|                            +----------+----------+                             |
|                            |  Serial I/O Worker  |                             |
|                            |  (pyserial Wrapper) |                             |
|                            +----------+----------+                             |
|                                       | /dev/ttyUSB0                           |
+---------------------------------------|----------------------------------------+
| Raw Binary Frame (16 bytes)
▼
[ USB to RS-485 Dongle ]

```

## 3. 핵심 컴포넌트 역할 설명

### 1) Local IPC Listener (소켓 수신 스레드)
- 외부 CLI 툴이나 타 로컬 프로세스로부터 `{"cmd": "set_temp", "id": 1, "value": 24}` 형태의 JSON 제어 명령을 수신합니다.
- 수신 즉시 명령어를 파싱하여 내부 `Command Queue`에 높은 우선순위로 인입합니다.

### 2) Command Queue (우선순위 큐)
- 폴링 작업과 제어 작업의 충돌을 피하기 위한 버퍼 역할을 합니다.
- 제어 명령 패킷이 들어올 경우 폴링 사이클보다 먼저 처리할 수 있도록 설계합니다.

### 3) Master Polling Thread (폴링 스케줄러)
- `config.py`에 등록된 활성 실내기 ID 목록을 순회하며 주기적으로(예: 3초 간격) "상태 요청(Poll)" 패킷을 준비합니다.
- `Serial Mutex`를 획득하여 시리얼 포트 전유권을 획득한 뒤 송수신을 진행합니다.

### 4) Serial Mutex (상호 배제 락)
- 반이중(Half-Duplex) 통신에서 TX와 RX가 엉키지 않도록 보호하는 핵심 동기화 장치입니다.
- 한 번에 단 하나의 패킷 송수신 트랜잭션(송신 완료 후 즉각 응답 수신 대기 타임아웃 종료까지)만 보장합니다.

### 5) Serial I/O Worker (드라이버 레이어)
- `pyserial`을 Wrapping한 최하단 컴포넌트입니다.
- **물리 예외 처리**: 포트 단절 시 지수 백오프로 재시도하고, 응답 패킷 유입 시 16바이트의 데이터 무결성을 실시간 검증합니다.

## 4. 데이터 흐름 (Data Flow Sequence)

### A. 일반적인 상태 폴링 루프
1. `Polling Thread`가 실내기 #1 상태 조회 요청 생성.
2. `Serial Mutex` Lock 획득.
3. `/dev/ttyUSB0`로 16바이트 Poll 패킷 전송 (TX).
4. 수신 버퍼 대기 (Timeout 150ms 적용).
5. 에어컨으로부터 16바이트 응답 패킷 수신 (RX).
6. 패킷 정합성 및 XOR 체크섬 검증 ➔ 정상이면 데이터 로그 및 내부 상태 갱신.
7. `Serial Mutex` Lock 해제 ➔ 다음 주기가 도래할 때까지 슬립.

### B. 제어 명령 주입 시나리오
1. `Local IPC Listener`를 통해 "온도 22도 설정" 명령 유입 ➔ `Command Queue`로 적재.
2. `Polling Thread`가 다음 폴링을 돌기 전 `Command Queue`를 확인.
3. 대기 중인 제어 명령 존재 시, 일반 폴링 대신 "제어 명령 패킷" 조립.
4. `Serial Mutex` Lock 획득 후 제어 패킷 전송 (TX).
5. 에어컨의 제어 완료 응답(ACK 패킷) 수신 대기 및 검증 (RX).
6. 처리 완료 후 `Serial Mutex` 해제 및 폴링 루프 복귀.

```

---

### 4. `ADR-001.md` (아키텍처 결정 기록)

```markdown
# ADR-001: ESPHome/MQTT 브릿지에서 라즈베리파이 RS-485 유선 직접 제어 파이썬 데몬으로의 피벗

## 1. 콘텍스트 (Context)
초기 설계에서는 ESP32 칩에 ESPHome 펌웨어를 올리고 Wi-Fi 무선 통신을 거쳐 MQTT 브로커를 활용하는 다단계 무선 브릿지 아키텍처를 계획하였습니다. 
그러나 실제 산업 및 상용 에어컨 제어 환경을 정밀하게 재분토한 결과 아래와 같은 치명적인 한계점이 노출되었습니다:
1. **무선 인프라 불안정성**: 철골 구조물과 실외기실의 밀폐 구조 특성상 Wi-Fi 도달률이 떨어져 통신 손실 가능성이 큼.
2. **다중 장애 지점(SPOF)**: ESP32 하드웨어 고장, 공유기 중단, MQTT 브로커 크래시 등 제어 채널 내 거쳐 가야 할 중개 프로세스가 너무 많음.
3. **유지보수 코스트**: ESP32의 C++ 소스 빌드 및 OTA 업데이트, 펌웨어 관리 코스트가 중복 발생함.

따라서, 이 모든 중개 레이어를 걷어내고 실외기 PCB에 있는 RS-485 통신 단자(CENA, CENB)에 안정적인 절연 컨버터를 이용해 라즈베리파이를 유선으로 직결하기로 결정하였습니다.

## 2. 아키텍처 결정 (Decision)
- **결정 사항**: 무선 게이트웨이 및 MQTT 중개 구조를 완전히 전면 폐기하고, 라즈베리파이(Linux) 환경에서 `pyserial`을 사용하여 LGAP 바이너리 프로토콜을 밑바닥부터 직접 파싱 및 폴링 제어하는 **단독 파이썬 데몬(python-raw-lgap-daemon)**을 구축한다.

## 3. 대안 분석 및 비교

| 비교 지표 | 대안 A: ESPHome + MQTT (기존) | 대안 B: Python Raw RS-485 직접 제어 (결정) |
| :--- | :--- | :--- |
| **물리적 안정성** | ⚠️ 낮음 (Wi-Fi 무선 간섭 및 패킷 손실) |  **매우 높음** (100% 산업용 유선 노이즈 차단 배선) |
| **시스템 복잡도** | ⚠️ 높음 (ESP32 펌웨어 + MQTT 브로커 필요) |  **낮음** (라즈베리파이-컨버터-실외기 단순 직결) |
| **구현 난이도** |  낮음 (esphome-lgap 오픈소스 활용) | ⚠️ 높음 (바이너리 체크섬 및 폴링 엔진 자체 구현) |
| **장애 조치 편의성**| ⚠️ 까다로움 (원격 하드웨어 리셋 곤란) |  **매우 쉬움** (systemd로 프로세스 자동 리스타트 가능) |
| **PoC 실효성** | 중간 (프로토타입 작동 증명 수준) |  **매우 우수** (실제 빌딩 제어 시스템과 호환 규격 입증) |

## 4. 결과 및 트레이드 오프 (Consequences)

### 긍정적 측면 (Pros)
- 무선 네트워킹 불안정이 완전히 제거되어 유선 레벨의 패킷 신뢰성 확보.
- 관리 대상 장비가 라즈베리파이 1대로 한정되어 상용 배포 및 운영 유지보수가 극도로 단순해짐.
- ESPHome의 제한된 기능에 가둘 필요 없이 파이썬의 강력한 기능을 활용해 프로토콜 고도화 및 커스텀 비즈니스 로직 작성 용이.

### 부정적 측면 (Cons)
- C++ ESPHome 라이브러리가 뒤에서 대신 처리해 주던 **'바이너리 바이트 처리, XOR 체크섬 검증, 반이중 시간차 제어'**를 파이썬 소프트웨어 코드 레이어에서 완전히 제로 베이스로 정밀하게 구현해야 하는 기술적 부하 발생.
- 실외기 PCB 단자까지 직접 배선해야 하므로 실 시공 노력이 발생함 (팀 내 장비 전문가가 동반되므로 소프트웨어 팀 리스크는 제한적임).

```

---

### 5. `code-guide.md` (코드 스타일 및 개발 규칙 정의서)

```markdown
# AI Code Generation Guidelines for python-raw-lgap-daemon

본 문서는 Antigravity IDE를 사용하는 개발자 및 협업 AI 모델이 본 프로젝트의 코드를 생성, 수정 및 리팩토링할 때 예외 없이 100% 준수해야 하는 강제 규칙 가이드입니다.

## 1. 기본 스타일 규칙
- **언어 사양**: Python 3.10 이상 문법을 준수합니다.
- **타입 힌팅**: 모든 함수의 매개변수와 반환값에는 엄격한 타입 힌팅을 적용합니다. (예: `def calculate_checksum(data: bytes) -> int:`)
- **외부 종속성 배제**: 특수한 사유가 없는 한 외부 라이브러리는 `pyserial`로 한정하며, 가급적 파이썬 표준 라이브러리(`threading`, `queue`, `socket`, `logging` 등)만을 사용하여 가볍고 순수한 코드를 지향합니다.

## 2. LGAP 시리얼 통신 특화 가이드

### 2.1 패킷 16바이트 정합성 및 프레이밍
- RS-485 버스는 언제나 불특정 바이트 노이즈가 유입될 수 있습니다. 단순 `read(16)`를 수행할 경우 프레임 싱크가 뒤틀릴 우려가 있습니다.
- **동기화 알고리즘 규칙**: 
  - 버퍼에서 바이트 단위로 읽어 들이면서, LGAP 패킷의 고유 시작 바이트 패턴(Header, 일반적으로 문헌상 정의된 특정 헤더 바이트)을 감지합니다.
  - 헤더 감지 시점부터 정확히 16바이트를 수집하여 1개의 패킷 프레임을 완성합니다.
  - 16바이트 프레임 완성 직후 즉각 XOR 체크섬을 수행하고, 유효하지 않은 패킷은 즉시 폐기하고 다음 헤더를 찾도록 설계합니다.

### 2.2 XOR 체크섬 검증 로직
- 패킷은 정확히 16바이트의 고정 길이입니다.
- **수식 규칙**:
  ```python
  # packet은 16바이트 크기의 bytes 타입 객체
  payload_sum = sum(packet[0:15]) % 256
  checksum = payload_sum ^ 0x55
  is_valid = (checksum == packet[15])

```

* 위 수학적 체크섬 룰을 유틸리티 모듈에 구조화하여, 송수신 전 필수 검증 단계로 태깅해야 합니다.

### 2.3 반이중(Half-Duplex) 락(Lock) 무조건 준수

* 시리얼 객체(`serial.Serial`)에 직접 접근하여 `write`와 `read`를 처리하는 구역은 오직 하나의 스레드 락(`threading.Lock`) 소유 하에서만 순차적으로 접근해야 합니다.
* **패턴**:
```python
with self.serial_lock:
    self.serial.write(tx_packet)
    # 반이중 충돌을 피하기 위해 즉각 송신 버퍼를 플러시(Flush)한 후 리드를 준비합니다.
    self.serial.flush() 
    rx_packet = self.serial.read(16)

```



## 3. 방어적 예외 처리 (Robust Exception Handling)

* **`serial.SerialException` 분리**: 포트 연결 해제 등 하드웨어 예외는 반드시 로깅하고, 데몬을 완전히 죽이지 않고 포트 클로즈 후 지수 백오프 기반 재시도 메커니즘(`while not reconnected: sleep(backoff)...`)을 보장합니다.
* **`print()` 금지**: 디버깅 용도를 포함하여 소스 코드 전체에 단순 `print` 사용을 절대 금지합니다. 모든 메타정보 및 패킷 흐름은 파이썬 표준 `logging` 객체를 사용하여 로깅 레벨(INFO/DEBUG)을 정의하고 체계적으로 출력해야 합니다.
* 패킷 송수신 로그 출력 시 가독성을 위해 Hex 포맷(`packet.hex()`)으로 일목요연하게 찍히도록 일관되게 구조화합니다.



---

### 🚀 AI PM의 다음 단계를 위한 가이드 안내

위의 6가지 핵심 설계 산출물 작성이 완료되었습니다. 프로젝트의 뼈대와 코딩 규칙이 완벽하게 정리되었기 때문에, 이제 Antigravity IDE를 여시고 본격적인 코드 생성 작업을 시작할 수 있는 최상의 조건이 갖춰졌습니다.

다음 단계인 **"Phase 1: 환경 구축 및 루프백 검증"**과 **"Phase 2: LGAP 프로토콜 코어 파서 구현"**의 성공적인 진행을 위해, **첫 번째 IDE 지시용 프롬프트**를 준비해 드릴까요? 준비가 되셨다면 말씀해 주세요. 즉시 가장 정밀하고 디테일한 프롬프트를 작성하여 전달해 드리겠습니다!
