# Reference Check Report: LGAP GitHub Source Code Specification (v2)

본 문서는 `reference/esphome-lgap` 오픈소스 저장소의 C++ 소스 코드(`.cpp`, `.h`) 분석을 통해 추출한 LGAP(LG Aircon Protocol)의 로우 레벨 바이너리 명세 및 핵심 알고리즘 정의서입니다. 기존의 MQTT 미들웨어 관련 기술 아키텍처를 전면 배제하고, 파이썬 기반 직접 제어 데몬 개발 시 AI가 토큰 낭비 없이 상시 참조할 수 있도록 순수 프로토콜 레이어의 명세만을 압축하여 기록합니다.

---

## 1. 물리 계층 및 프레임 동기화 사양 (Physical & Link Layer)

- **물리 인터페이스**: RS-485 유선 통신 (실외기 메인 PCB의 CENA, CENB 단자 직결)
- **시리얼 통신 설정**: Baud rate **4800 bps**, Data bits **8**, Parity **None**, Stop bits **1** (8N1)
- **통신 제약**: 반이중(Half-Duplex) 통신 환경으로, 데이터 송신(TX)과 수신(RX)이 동일한 선로를 공유함.
- **패킷 데이터 크기**: **16바이트 고정 크기(Fixed-size)** 바이너리 프레임 구조.
- **프레임 동기화 (Framing)**: 버스 노이즈로 인한 바이트 밀림을 방지하기 위해, 스트림 가동 시 패킷의 고유 헤더 바이트(Header Byte) 패턴을 상시 탐색하는 슬라이딩 윈도우 알고리즘 검증 필수.

---

## 2. 체크섬 알고리즘 명세 (XOR Checksum)

LGAP 프로토콜은 데이터 무결성을 검증하기 위해 Modulo 256 합산 값과 특정 비트마스크의 XOR 연산을 조합한 고유 체크섬을 사용합니다.

- **알고리즘 정의**: 16바이트 패킷 중 마지막 바이트(Index 15)를 제외한 앞선 1~15바이트(Index 0 ~ 14)의 전체 합을 구한 뒤, 이를 256으로 나눈 나머지(Modulo 256)를 구합니다. 최종적으로 이 결과값에 `0x55` 바이트를 XOR 연산하여 체크섬을 산출합니다.
- **수식 표현**:
  $$\\text{Checksum} = \\left( \\sum_{i=0}^{14} \\text{Byte}[i] \\pmod{256} \\right) \\oplus 0\\text{x}55$$
- **파이썬 구현 가이드라인**:

```

```text
File generated successfully.

```python
  def calculate_checksum(packet: bytes) -> int:
      if len(packet) < 15:
          raise ValueError("패킷 크기가 체크섬 계산에 불충분합니다.")
      payload_sum = sum(packet[0:15]) % 256
      return payload_sum ^ 0x55

```

---

## 3. 핵심 데이터 인코딩 & 디코딩 공식 (Data Conversion)

C++ 소스 코드 핵심 컴포넌트 분석을 통해 규명된 로우 레벨 바이트와 실제 에어컨 물리 값 간의 변환 공식은 다음과 같습니다.

### 3.1 설정 온도 (Target Temperature)

* **위치**: 수신 패킷 바이트 7 (RX Byte 7) 및 송신 제어 패킷의 매핑 구역.
* **디코딩 공식**: 하위 4비트(비트마스크 `0x0F`)를 취한 후 15를 더함.

* Celsius = (raw_byte & 0x0F) + 15

* **유효 범위**: 16°C ~ 30°C (가드 클로즈 처리를 통해 해당 범위 외 제어 명령 차단 필수).

### 3.2 실내 온도 및 배관 온도 (Room & Pipe Temperature)

* **위치**: 수신 패킷 바이트 8 ~ 10 (RX Byte 8: 실내 온도, Byte 9~10: 열교환기 배관 온도 센서 값).
* **디코딩 공식**: 정수 나눗셈에 의한 소수점 오차 및 음수 표현 왜곡을 방지하기 위해 C++ 소스 코드에 탑재된 전용 오프셋 역산 공식 준수.

* Celsius = (192 - raw_byte) / 3

* **주의 사항**: 파이썬 구현 시 부동소수점 오차 정밀화를 위해 `round(val, 1)` 처리를 동반할 것.

---

## 4. 제어 비트 플래그 및 상태 매핑 규칙 (Bitmask Tables)

### 4.1 가동 전원 (Power & Execution Flag)

* 에어컨의 상태가 변경되거나 사용자가 제어 명령(Write)을 내릴 때, 패킷 내의 특정 실행 업데이트 비트 플래그(`EXE = 1`)가 셋업되어야 실외기 메인 컨트롤러가 명령을 접수함.
* 단순 조회 폴링 시에는 해당 비트 플래그가 `0`으로 유지되어 수동 모니터링 상태를 지속함.

### 4.2 운전 모드 (Operating Mode)

바이트 내 특정 비트 영역의 매핑 값을 바탕으로 에어컨의 운전 모드를 판별 및 인코딩합니다.

* `0x00`: 냉방 (Cool)
* `0x01`: 탈탈습 / 제습 (Dry)
* `0x02`: 송풍 (Fan Only)
* `0x03`: AI / 자동 (Auto)
* `0x04`: 난방 (Heat)

### 4.3 풍량 설정 (Fan Speed)

* `0x01`: 약풍 (Low)
* `0x02`: 약풍/중풍 사이 혹은 자동 (Auto/Natural)
* `0x04`: 중풍 (Medium)
* `0x08`: 강풍 (High)
* `0x10`: 파워풍 / 제트쿨 (Power)

---

## 5. 코드 포팅 시 제약 사항 (Implementation Constraints)

1. **정수 오버플로우 방지**: C++ 소스 코드 내에서 `uint8_t` 데이터 타입을 기반으로 비트 연산 및 체크섬 합산이 이루어지므로, 파이썬 코딩 시에도 각 바이트 연산 결과에 상시 `& 0xFF` 연산을 적용하여 8비트 레벨의 언더플로우/오버플로우 환경을 완벽하게 모킹해야 함.
2. **패킷 타이밍 마진**: 반이중 시리얼 통신의 특성상, 16바이트 제어 명령 송신 직후 에어컨 측 내부 릴레이 및 컨트롤러가 수신 모드로 전환하는 데 최소 수 밀리초(ms)의 딜레이가 소요되므로, 송신 완료 직후 `serial.flush()` 호출 및 미세 지연 후 리드 버퍼를 여는 하드웨어 친화적 시퀀스를 충족해야 함.
