**실제로 수행하셔야 하는 하드웨어 결선 및 현장 테스트 마스터 플랜**

---

## 🛠️ 현실 세계의 미션: 하드웨어 결선 및 최종 런타임 테스트 절차

사용자분께서 말씀하신 대로, LG 시스템 에어컨(멀티형 또는 상업용)의 경우 실내기가 아닌 **실외기 메인 PCB 박스 내부**에 RS-485 통신선(`CENA`, `CENB` 단자)이 배치되어 있거나, `PI-485`라는 별도의 통신 확장 보드를 실외기에 장착하도록 되어 있는 경우가 많습니다.

이 환경을 기준으로 현장에서 안전하고 무결하게 연동을 끝내는 4단계 실행 팩입니다.

### Phase 1: 실외기 하드웨어 결선 및 물리 계층 구축

1. **에어컨 전원 완전 차단 (최우선)**
* **행동:** 집 안이나 건물의 메인 분전반(두꺼비집)에서 에어컨 '실외기 및 실내기 전원 차단기'를 모두 내립니다.
* **이유:** 실외기 내부에는 고전압 대용량 커패시터와 220V/380V 동력선이 지나가므로 감전 위험이 매우 크며, 기판 쇼트 시 메인 PCB가 즉사합니다.


2. **실외기 제어반(Control Box) 개방 및 단자 탐색**
* **행동:** 실외기 전면 또는 측면의 나사를 풀어 철제 커버를 열고 메인 PCB 기판을 확보합니다.
* **행동:** 기판 실크스크린 인쇄 중 **`CENA` (또는 TRX+)**, **`CENB` (또는 TRX-)** 라고 적힌 RS-485 통신 단자를 찾습니다. (또는 기 장착된 PI-485 보드의 단자 확인)


3. **RS-485 to UART 하드웨어 브릿지 연결**
* **행동:** 에어컨의 `CENA`, `CENB` 단자선을 ESP32 기기와 연결하되, 중간에 **MAX485 같은 RS-485 to TTL 변환 모듈**을 경유하여 연결합니다.
* **핀 매핑:**
* 에어컨 `CENA` ➔ 변환 모듈의 `A` 핀
* 에어컨 `CENB` ➔ 변환 모듈의 `B` 핀
* 변환 모듈의 `RX`/`TX` ➔ ESP32의 `TX`/`RX` 핀 (교차 결선)
* 변환 모듈 및 ESP32의 `GND` ➔ 에어컨 기판의 공통 `GND`




4. **실외기 전용 방수/방진 처리**
* **행동:** 실외기는 항상 야외(눈, 비, 습기, 고온)에 노출되므로, ESP32 보드와 변환 모듈을 반드시 IP66 등급 이상의 플라스틱 방수 하이박스(밀폐 케이스)에 넣고 실외기 내부 빈 공간에 단단히 고정합니다.



---

### Phase 2: 인프라 가동 (우체국 개점)

1. **중앙 MQTT 브로커(서버) 구동**
* **행동:** 라즈베리파이 혹은 로컬 서버에 `Mosquitto` 같은 MQTT 브로커를 설치하고 가동합니다.
* **행동:** 외부 기기(ESP32, 다른 PC)가 접속할 수 있도록 `mosquitto.conf` 파일에 `listener 1883 0.0.0.0` 및 `allow_anonymous true` 설정을 적용하고 서비스를 재시작합니다.



---

### Phase 3: 가상 시뮬레이터 런타임 사전 검증 (방구석 예행연습)

실제 실외기에 무선으로 붙이기 전, 우리가 만든 프로그램이 완벽한지 개발 PC나 라즈베리파이 내부에서 먼저 실시간 연동을 눈으로 확인합니다.

1. **터미널 1: 가상 에어컨 발생기 가동**
```bash
python mock_bridge.py

```


* 이 스크립트가 3초마다 가짜 에어컨 온도 데이터를 MQTT 브로커로 발행하기 시작합니다.




2. **터미널 2: 메인 파이썬 데몬 엔진 가동**
```bash
python main.py

```


* 메인 엔진이 켜지면서 `mock_bridge.py`가 던지는 온도 JSON 데이터를 실시간으로 수신하여 파싱하고 `app_logger`를 통해 화면에 일목요연하게 출력하는지 관측합니다.




3. **제어 피드백 루프 테스트**
* 
`main.py` 내부에서 가상의 제어 명령(예: 냉방 24도 설정)을 던졌을 때, `mapping_helper.py` 가드 클로즈가 정상 통과되어 `set` 토픽으로 날아가고 , `mock_bridge.py` 측 터미널에 "가상 에어컨 변경 완료" 피드백이 실시간으로 상호 교차 출력되는지 확인합니다. 이 루프가 돌면 소프트웨어 검증은 완전히 끝난 것입니다.





---

### Phase 4: 실기기 최종 현장 배포

1. **ESPHome 펌웨어 주입 및 부팅**
* **행동:** PC에서 ESP32 보드에 `esphome-lgap` C++ 컴파일 펌웨어를 굽습니다. 이때 Wi-Fi 비밀번호와 Phase 2에서 개설한 MQTT 브로커 IP 주소를 YAML 설정에 정확히 기입해야 합니다.
* **행동:** 실외기 차단기를 올리고 전원을 켭니다.


2. **라즈베리파이 데몬 서비스 최종 활성화**
* 
**행동:** 라즈베리파이 터미널에서 에이전트가 만들어 준 서비스 파일을 리눅스 시스템에 등록하고 상시 기동합니다.




```bash
sudo cp lgap_poc.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable lgap_poc.service
sudo systemctl start lgap_poc.service

```


3. **로그 모니터링**
* 아래 명령어를 통해 프로그램이 백그라운드에서 실외기로부터 실시간으로 올라오는 진짜 온도 데이터를 추적하고 제어 패킷을 올바르게 중계하는지 확인합니다.


```bash
sudo journalctl -u lgap_poc.service -f

```

**리눅스 시리얼 포트 접근 권한 (dialout 그룹)**
라즈베리파이에 USB to RS-485 컨버터를 꽂으면 /dev/ttyUSB0 기기 파일이 생성됩니다. 리눅스 보안 정책상 일반 유저(예: pi 또는 ubuntu)는 이 파일에 직접 읽기/쓰기를 할 수 없습니다.

실장 테스트 시 반드시 라즈베리파이 터미널에서 sudo usermod -aG dialout <유저명> 명령을 실행하여 유저에게 시리얼 포트 접근 권한을 부여해야 데몬이 권한 에러로 뻗지 않습니다.

---