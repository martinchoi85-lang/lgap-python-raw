# 현장 하드웨어 결선 및 테스트 실행 가이드 (Field Test Master Plan)

> **[핵심 안내] 아키텍처 피벗 반영 완료**
> 과거 구상했던 **ESP32 보드, MAX485 TTL 모듈, ESPHome 펌웨어, Mosquitto MQTT 브로커**는 모두 **사용하지 않습니다**.
> 
> 현재 구조는 **[실외기 CENA/B 단자] ➔ [전선 2가닥] ➔ [USB to RS-485 컨버터] ➔ [회사 업무용 노트북 USB]** 로 초간소화된 **유선 직결 구조**입니다.

---

## 1. 하드웨어/통신 초보자를 위한 핵심 개념 3분 요약

### ① RS-485 통신이란 무엇인가요?
* 에어컨이나 공장 기계에서 널리 쓰이는 **산업용 유선 통신 규격**입니다.
* 전선 2가닥(`A`선, `B`선)의 **전압 차이**를 이용해 `0`과 `1`의 디지털 데이터를 주고받습니다.
* 에어컨 실외기 기판에 있는 **`CENA` (또는 TRX+)** 단자가 `A`선이고, **`CENB` (또는 TRX-)** 단자가 `B`선입니다.

### ② 'RS-485 to UART 하드웨어 브릿지' 대신 우리가 사용하는 것
* **USB to RS-485 컨버터 (USB 동글)** 하나만 있으면 됩니다.
* 납땜이나 복잡한 회로 구성(ESP32, MAX485 칩 등)이 전혀 필요 없습니다.
* 컨버터 한쪽은 일반 **USB 단자**(노트북에 연결), 반대쪽은 **A / B 나사 단자대**(에어컨 전선 2가닥을 나사로 조여 고정)로 되어 있습니다.

```
[ LG 실외기 기판 ]                     [ 2가닥 통신선 ]                [ USB-to-RS485 컨버터 ]        [ 회사 노트북 ]
┌─────────────────┐                  ┌─────────────┐               ┌───────────────────────┐      ┌─────────────┐
│  단자 CENA (A+)  ├──────────────────┤   A 전선    ├───────────────┤ A(D+) 단자             │      │             │
│                 │                  │             │               │           [USB 플러그] ├──────┤ USB 포트     │
│  단자 CENB (B-)  ├──────────────────┤   B 전선    ├───────────────┤ B(D-) 단자             │      │ (Python 실행)│
└─────────────────┘                  └─────────────┘               └───────────────────────┘      └─────────────┘
```

### ③ MQTT 브로커(Mosquitto)는 왜 필요 없나요?
* MQTT는 무선 Wi-Fi 환경에서 데이터를 중계해 주는 "인터넷 우체국 서버"입니다.
* 지금은 **노트북이 USB 케이블로 실외기와 직접 1:1 유선 연결**되어 있으므로, 중간 서버(MQTT)나 공유기, Wi-Fi가 전혀 필요 없습니다.
* 노트북에서 파이썬 프로그램(`main.py`)을 켜기만 하면 에어컨과 즉시 직접 대화합니다.

---

## 2. 현장 준비물 체크리스트

1. **회사 업무용 노트북** (Python 3.10+ 및 본 소스코드 설치)
2. **USB to RS-485 절연(Isolated) 컨버터** (FTDI / CH340 / CP2102 칩셋 기반)
3. **2가닥 연선(Twisted Pair) 케이블** (랜선 UTP 1페어 또는 2심 실드선, 2~5미터 내외)
4. **소형 일자/십자 드라이버** (실외기 커버 개방 및 컨버터 단자 나사 조임용)
5. **목장갑 / 절연 장갑** (현장 안전용)

---

## 3. 현장 실전 4단계 실행 절차

### Phase 1: 안전 확보 및 실외기 단자 탐색 (최우선)

1. **에어컨 전원 차단기 내리기**
   * **행동:** 분전반(두꺼비집)에서 에어컨 **실외기 및 실내기 차단기를 반드시 내립니다**.
   * **이유:** 실외기 내부에는 220V/380V 고전압 동력선과 대용량 커패시터가 있어 감전 위험이 있습니다.
2. **실외기 제어반(커버) 개방**
   * **행동:** 실외기 전면/측면 나사를 풀고 커버를 열어 메인 PCB(전자 기판)를 확인합니다.
3. **통신 단자 확인**
   * **행동:** 기판 표면에 인쇄된 글씨 중 **`CENA`**, **`CENB`** 단자를 찾습니다. (또는 기판에 장착된 `PI-485` 게이트웨이 보드의 A/B 단자)

---

### Phase 2: 통신선 2가닥 결선 (초간단)

1. **전선 연결**
   * 전선 1번 가닥: 실외기 **`CENA`** ➔ USB 컨버터의 **`A` (또는 `D+`, `A+`)** 단자에 넣고 나사 조임
   * 전선 2번 가닥: 실외기 **`CENB`** ➔ USB 컨버터의 **`B` (또는 `D-`, `B-`)** 단자에 넣고 나사 조임
   *(참고: A와 B가 서로 바뀌어 꽂히더라도 기기가 고장 나지 않으며, 통신이 안 될 경우 나중에 두 가닥만 서로 맞바꿔 꽂으면 됩니다.)*
2. **에어컨 전원 차단기 올리기**
   * 배선이 끝났으면 실외기 내부 부품에 닿지 않도록 선을 정리하고 에어컨 차단기를 다시 올립니다.

---

### Phase 3: 회사 노트북 세팅 및 COM 포트 지정

1. **노트북에 컨버터 꽂기**
   * USB to RS-485 컨버터를 회사 노트북 USB 포트에 꽂습니다.
2. **장치 관리자에서 포트 번호 확인**
   * Windows 키 + X ➔ **[장치 관리자]** 실행 ➔ **[포트 (COM & LPT)]** 클릭
   * 예: `USB Serial Port (COM3)` 확인 (여기서 `COM3` 기억)
3. **설정 파일 수정**
   * `config.py` 파일의 포트 번호를 변경합니다:
     ```python
     SERIAL_PORT = "COM3"  # 확인한 포트 번호로 입력
     BAUD_RATE = 4800      # 4800 bps 유지
     ```
   * `main.py` 파일 하단의 `run_daemon()` 실행부에서 mock 모드를 끕니다:
     ```python
     daemon = LGAPDaemon(use_mock=False)  # 실제 실외기 통신 모드
     ```

---

### Phase 4: 데몬 실행 및 실물 패킷 검증

1. **데몬 실행**
   ```powershell
   # 가상환경 활성화 후 실행
   .\venv\Scripts\activate
   python main.py
   ```
2. **로그 관측 및 정상 여부 판정**
   * **[정상 동작 시]**:
     ```text
     [INFO] [PollingEngine] Polling Unit 0x00...
     [DEBUG] [SerialWorker] [POLL TX] 02 00 00 00 ... (16 bytes)
     [DEBUG] [SerialWorker] [POLL RX] 02 00 00 ... (16 bytes) -> Checksum OK
     [INFO] [State] Unit 0x00: Power=ON, Mode=COOL, SetTemp=24°C, RoomTemp=26°C
     ```
   * **[응답 없음 / 타임아웃 시 체크포인트]**:
     1. 전선 `A`와 `B`의 극성이 바뀌었을 수 있습니다. 차단기를 내리고 컨버터 쪽 `A`와 `B` 선을 맞바꿔 꽂아봅니다.
     2. 실내기 주소(ID)가 `0x00`이 아닐 수 있습니다. `config.py`의 `TARGET_UNITS = [0x00, 0x01, 0x02]` 등으로 대상을 넓혀봅니다.
3. **온도 제어 테스트**
   * 새 터미널 창을 열고 HTTP 제어 명령을 전송해 봅니다:
     ```powershell
     # 실내기 0번 22도 냉방 설정
     curl -X POST http://127.0.0.1:8080/control -H "Content-Type: application/json" -d "{\"unit_id\": 0, \"power\": true, \"mode\": \"COOL\", \"set_temp\": 22}"
     ```
   * 에어컨 실내기에서 "삐빅" 비프음이 울리며 설정 온도가 22도로 변경되는지 확인합니다.

---

## 4. 요약: 현장 작업 흐름 한 줄 요약

> **차단기 내림 ➔ CENA/B에 전선 2가닥 연결 ➔ 컨버터 물려서 노트북 USB 연결 ➔ 차단기 올림 ➔ 장치 관리자 COM 포트 확인 ➔ `python main.py` 실행 ➔ 패킷 수신 확인!**